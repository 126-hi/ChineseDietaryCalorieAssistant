# ── Core ─────────────────────────────────────────────
streamlit>=1.34
openai>=1.25
tiktoken>=0.5.1          # for OpenAIEmbeddings

# ── LangChain Stack ───────────────────────────────────
langchain==0.2.*
langchain-core>=0.2
langchain-community>=0.0.8
faiss-cpu>=1.8.0         # Vector store for retrieval (RAG)

# ── PDF parsing for RAG ───────────────────────────────
pypdf>=3.15              # Lightweight PDF loader

# ── Data & Visualisation ──────────────────────────────
pandas>=2.2
plotly>=5.20
pillow>=10.0             # Image support for Streamlit

# ── HTTP & Environment ────────────────────────────────
requests>=2.31
python-dotenv>=1.0       # For environment variable management

# ── YOLOv7 Dependencies ───────────────────────────────
# Install CPU-only version of torch (compatible with Codespaces)
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu
-f https://download.pytorch.org/whl/cpu/torch_stable.html

opencv-python>=4.9
numpy>=1.25

# ── Important Notes ───────────────────────────────────
# ⚠️ System Dependency:
#   If you encounter ImportError: libGL.so.1 missing (typically on Linux / Codespaces),
#   please install system package manually:
#   
#   sudo apt update && sudo apt install -y libgl1-mesa-glx
#
# ⚠️ YOLOv7 Local Repository:
#   This project requires yolov7 source code locally.
#   Please clone yolov7 repository into project directory:
#
#   git clone https://github.com/WongKinYiu/yolov7
#
#   Ensure yolov7/ contains: models/, utils/, detect.py etc.


