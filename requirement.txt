# ── Core ──────────────────────────────────────────────────────────────
streamlit>=1.34
openai>=1.25

# LangChain stack (v0.2)
langchain==0.2.*              # orchestrator
langchain-core>=0.2
langchain-community>=0.0.8
faiss-cpu>=1.8.0              # vector store

# PDF parsing for RAG
pypdf>=3.15                   # lightweight reader

# Data & visualisation
pandas>=2.2
plotly>=5.20
pillow>=10.0                  # image display

# HTTP requests (HuggingFace/Nutritionix)
requests>=2.31

# Optional: .env file loading (if you prefer env vars over sidebar inputs)
python-dotenv>=1.0
